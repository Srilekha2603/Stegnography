# Stegnography
This project implements Image Steganography to securely hide secret data inside an image file. 
The program uses the Least Significant Bit (LSB) technique to embed a secret text file into a BMP image without visibly changing the image. 
It supports both encoding (hiding data inside an image) and decoding (extracting hidden data from the image).
